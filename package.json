{
  "name": "shortify-my-url-chrome-extension",
  "version": "1.0.0",
  "description": "Chrome extensions for quick access to Shortify My URL!",
  "main": "index.js",
  "scripts": {
    "pack": "parcel build src/index.html --no-source-maps --no-cache",
    "copy-files": "cp './src/manifest.json' './dist' && cp './src/images/icon16.png' './dist'",
    "build": "npm run pack && npm run copy-files"
  },
  "keywords": [],
  "author": "Ivan Tachenko",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "axios": "^0.21.0",
    "parcel-bundler": "^1.12.4",
    "regenerator-runtime": "^0.13.7"
  }
}
